{
  "name": "date-app-server",
  "version": "1.0.0",
  "description": "server side application for date app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon --ignore '*.json' --ignore 'src/public' ./app.js",
    "prod": "cross-env NODE_ENV=production node ./app.js",
    "run": "cross-env NODE_ENV=production node ./app.js"
  },
  "author": "oscar martinez",
  "license": "PRIVATE",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.6",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "morgan": "^1.10.0"
  }
}
